export const environment = {
    production: false,
    // La URL de tu backend local
    apiUrl: 'http://localhost:3000/api',
    // Subdominio que se enviará como x-tenant-id cuando se corra en localhost
    // Cambia esto al subdominio de un tenant que exista en tu base de datos local
    devSubdomain: 'osotest1',
};
